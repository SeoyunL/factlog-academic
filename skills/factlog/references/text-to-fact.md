# Text-to-Fact 운영 기준

이 문서는 Claude Code가 `sources/`에서 fact 후보를 추출할 때 따르는 기준입니다.

## 역할

당신은 fact 추출자입니다. 문서를 요약하는 것이 아니라, 원문에서 직접 확인되는 관계 후보를 `facts/candidates.csv`에 기록합니다.

## 완전성 원칙 (가장 중요)

추출의 목표는 "요점 정리"가 아니라 **원문에서 검증 가능한 모든 관계를 빠짐없이** 기록하는
것입니다. 문서를 한 번 훑고 멈추지 말고, **모든 섹션·표·목록을 끝까지 순회**하십시오.

- **샘플링 금지**: 같은 유형의 항목이 여러 개면(참여자 N명, 기관 M개, 월별 일정 K개,
  경력·학력·특허·실적 목록 등) 대표 한두 개만 뽑지 말고 **전부** 추출합니다. "대표적으로
  몇 개만"은 누락이며, 자유 노트 위키보다 못한 결과를 만듭니다.
- **산문에서 멈추지 말 것**: 서술형 문단은 눈에 잘 띄어 먼저 추출되지만, fact 밀도가 가장
  높은 곳은 보통 **표**입니다. 표를 건너뛰면 대량 누락이 발생합니다.
- **크기로 판단 금지**: office/HWP 변환본은 표가 HTML 마크업으로 부풀려져 바이트 크기가
  큽니다(본문은 그보다 훨씬 적음). 추출 분량은 파일 크기가 아니라 **섹션·표 커버리지**로
  판단하십시오.

### 표·구조화 데이터 추출

표는 행마다 하나 이상의 관계 명제를 담는, 가장 fact 밀도가 높은 구조입니다. 다음과 같이
매핑해 **행 단위로 모두** 추출하십시오.

- 행을 식별하는 키(이름·기관명·연도 등) → `subject`
- 열 머리글 또는 항목명 → `relation`
- 셀 값 → `object`
- 한 항목에 여러 값·연도가 있으면(예: 매출 2023/2024/2025) 각각 **별도 행**으로 만들고,
  연도·단위 같은 맥락은 `note`에 둡니다.

명부, 재무·등기 현황, 예산 비목·세부명세, 추진 일정, 경력·학력·특허·정부사업 실적 같은
표는 특히 누락되기 쉬우니 반드시 행 단위로 훑습니다.

### 자기 점검 (추출 종료 전)

마치기 전에 스스로 확인하십시오: **"다루지 않은 섹션·표·목록이 남아 있는가?"** 남아 있으면
돌아가서 추출합니다. 단, 아래 **금지 항목**의 개인정보는 표를 행 단위로 훑되 추출 대상에서
제외합니다(전화번호·이메일·생년월일·주민등록번호·개인 주소). 기관의 사업자등록번호·법인
등록번호·재무 수치 등 공개된 사업 정보는 추출 대상입니다.

## 입력 범위

- 입력 문서는 `sources/` 아래의 파일만 사용합니다.
- `pages/`, `facts/`, `decisions/`의 기존 내용은 중복 확인과 갱신 판단에만 사용합니다.
- `sources/` 밖의 임시 파일, 로그, 개인 메모는 fact 추출 입력으로 사용하지 않습니다.

## 지원 파일 포맷

fact 추출은 `sources/` 아래 파일을 **텍스트로 읽어** 수행합니다. 엔진(`merge_candidates.py`)은 `sources/`의 모든 파일을 source 경로로 추적하지만 내용을 파싱하지는 않으므로, 텍스트로 읽히는 파일만 실제로 추출됩니다. 비텍스트 파일이 있으면 `merge_candidates.py`가 경고를 출력합니다.

- **직접 지원**: `.md`, `.markdown`, `.txt` 등 UTF-8 텍스트. 원문 그대로 읽습니다.
- **plain text로 지원**: `.rst`, `.org`, `.csv`, 소스 코드 등 그 밖의 UTF-8 텍스트. 별도 파싱 없이 raw text로 취급합니다.
- **`factlog ingest`로 자동 변환**: `.docx`, 바이너리 `.pdf`, `.odt`, `.epub`, `.html`, `.rtf`(pandoc·textutil·pdftotext), `.hwpx`(한컴 OWPML — 내장 추출기), `.hwp`(구 한컴 HWP 5.x — `hwp5html`(pyhwp)→pandoc→markdown, 표 보존; `pip install pyhwp` + pandoc 필요), 그리고 `.pptx`(PowerPoint OOXML — 내장 추출기, 슬라이드 순서대로 텍스트 추출). 변환본은 `runs/sources/`에 기록되어 추출에 쓰입니다.
- **텍스트 컨테이너(`.html`·`.htm`·`.rtf`)**: 바이트는 텍스트지만 내용은 마크업입니다. **변환 성공 여부와 무관하게 원본은 절대 추출 대상이 아닙니다** — 마크업을 산문으로 읽느니 아무것도 읽지 않는 편이 낫습니다. `factlog ingest` 가 만든 `runs/sources/` 변환본을 대신 읽으십시오. 변환본이 없으면 `merge_candidates` 가 경고하며, 그 소스는 사실 없이 남습니다.
- **직접 미지원(수동 변환 필요)**: `.xlsx`, 이미지. `sources/`에 그대로 두면 source 경로로만 등록되고 fact는 생성되지 않습니다(조용한 미인제스트).

## 출력 위치

- candidate fact는 `facts/candidates.csv`에 씁니다.
- 사람이 읽을 개념 설명은 `pages/`에 씁니다.
- 중복 개념, 애매한 관계명, 출처 부족, 충돌 후보는 `decisions/open-questions.md`에 씁니다.

## 검토 섹션

`decisions/open-questions.md`는 중복·모호·출처·충돌 **네 범주의 섹션을 항상 갖습니다**.
검토 대상이 아직 없어도 섹션 자체는 존재합니다. `factlog init`이 파일을 그렇게 만들고,
`merge_candidates`가 그 상태를 유지하며, `validate.py`가 같은 목록으로 검사합니다.

섹션 **heading 문자열의 단일 출처는 `factlog/review_sections.py`의
`REVIEW_CATEGORIES`**입니다. 스캐폴드·검증·bullet 배치 어디에도 heading 을 따로 적어
두지 마십시오 — 세 벌로 갈라져 있던 것이 #495의 원인입니다. (범주를 판정하는 쪽,
즉 `decision_section`과 `record_stale_page_refs`에는 `"출처"` 같은 **범주 키워드**가
남아 있습니다. 어떤 행이 어느 범주인지는 분류 규칙이라 그 자리에 있어야 하고, 그
키워드가 어떤 heading 으로 해석되는지는 `section_for`가 결정합니다.)

어떤 줄이 heading 이고 어디까지가 그 섹션인지, 즉 **구조 질문의 단일 출처는
`factlog/md_lines.py`** 입니다. 검토 범주를 이름 부르지 않는 질문(이 줄이 코드로
렌더링되는가, heading 이 어디서 시작하고 끝나는가, 어떤 줄이 리스트 항목인가)은 전부
그쪽이고, `review_sections.py` 는 그 답 중 네 범주에 해당하는 것만 고릅니다.

섹션으로 인정되는 것은 **코드펜스 밖의 레벨 2 heading**입니다. 두 표기 모두 셉니다.

- ATX: `## 출처 부족`
- 밑줄식(Setext): `출처 부족` 다음 줄에 `----`

밑줄은 `-` 여야 합니다. `====` 는 레벨 1이라 `# Open Questions` 와 같은 취급, 즉
제목이지 섹션이 아닙니다. 밑줄 앞 공백은 3칸까지 허용되고 4칸부터는 들여쓴
코드블록입니다. `-` 하나(`-`)로도 충분하고 `-=-` 처럼 섞이면 밑줄이 아닙니다.

밑줄이 밑줄이 되려면 **바로 위가 최상위 문단**이어야 합니다. 리스트 항목 밑의
`----` 는 heading 이 아니라 수평선입니다 — `- a` 다음 줄의 `출처`는 그 항목의 이어짐이고
그 아래 `----` 는 리스트 밖의 thematic break 입니다. 인용(`> `) 안, 코드펜스 안, 빈 줄
바로 아래도 마찬가지로 heading 이 아닙니다. 이 판정은 CommonMark 렌더러와 대조해
맞춘 것이라, **도구가 섹션이라고 세는 것과 사람이 렌더링해서 보는 것이 같습니다.**

> **동작 변경**(#500): 이전에는 ATX `## ` 만 섹션이었고 Setext 로 쓴 섹션은 없는 것으로
> 취급됐습니다. 그래서 손으로 밑줄식으로 쓴 파일은 네 범주가 전부 없다고 나오고,
> merge 가 ATX 섹션 네 개를 아래에 새로 만들어 범주마다 섹션이 두 벌이 됐습니다.
> 이제 인식하므로 그런 파일은 그대로 유지됩니다. 이미 두 벌이 된 파일은 아래
> `split_review_section` 경고로 보입니다.

코드펜스 안에 `## 출처 부족` 이나 `- needs_review: ...` 같은 **예시**를 적어 두는 것은
안전합니다. 도구는 그것을 섹션이나 기록된 bullet 으로 세지 않고, 새 bullet 도 거기로
보내지 않으며, 필요하면 진짜 섹션을 따로 만듭니다.

펜스는 CommonMark 규칙대로 **여는 마커와 같은 문자, 같은 길이 이상**인 마커로만 닫힙니다.
그러니 백틱이 들어간 예시를 적을 때 바깥을 `~~~` 로 감싸도 됩니다 — 안쪽의 ` ``` ` 은
그 블록을 닫지 않습니다.

**닫히지 않은 코드펜스가 있으면 도구는 이 파일에 아무것도 쓰지 않습니다.** 펜스 아래는
전부 코드로 읽히므로 그 뒤에 덧붙인 heading 은 다시 읽히지 않고, 거기 들어간 검토
bullet 은 렌더링에서 보이지 않습니다. `merge_candidates` 는 몇 번째 줄이 문제인지
경고하고 그 파일을 건너뛰며, `validate.py` 도 `warning: unclosed_fence:` 로 같은 줄을
가리킵니다. 펜스를 닫고 다시 돌리면 그대로 이어집니다. 그 사이에도 해당 행은
`facts/candidates.csv` 에 `needs_review` 로 남아 있으므로 검토 큐 자체는 사라지지
않습니다.

**rc 는 펜스가 어디서 열렸는지에 달려 있습니다.** 네 섹션 heading 보다 위에서 열려
그 heading 들이 가려지면 "섹션이 없다"는 오류로 rc=1 이 됩니다. 반대로 네 heading
**뒤**(예: 파일 끝에 붙여넣다 만 조각)에서 열리면 검사가 요구하는 것을 아무것도 가리지
않으므로 **rc=0 이고 경고만 남습니다.** 어느 쪽이든 파일에는 쓰이지 않으니, 이 경고는
rc 와 무관하게 읽어야 합니다.

> **동작 변경**: 이전에는 이런 파일에도 그냥 써서 검토 bullet 이 코드펜스 안에 묻힌 채
> rc=0 으로 끝났습니다. 이제는 쓰지 않고 경고합니다(rc 는 위 규칙대로).

새 bullet은 그 범주의 섹션이 **파일에 이미 있으면 표기가 달라도 그 섹션**으로 갑니다.
같은 범주의 섹션을 새로 만들지 마십시오. 한 범주가 두 섹션으로 갈라지면 위쪽은
"현재 없음"이라 말하는데 실제 검토 큐는 아래쪽에 쌓여, 사람이 위만 읽고 검토할 것이
없다고 오독합니다.

**이미 갈라진 파일은 도구가 합치지 않습니다.** 사람이 쓰고 분류해 둔 bullet 을 코드가
옮기는 일이기 때문입니다. 대신 `validate.py`가 `warning: split_review_section:` 으로
알려 주며(rc 는 움직이지 않습니다), 정리는 사람이 한 번 해야 합니다.

## CSV 스키마

`facts/candidates.csv`의 헤더는 반드시 다음 한 줄입니다.

```csv
subject,relation,object,source,status,confidence,note
```

각 행은 검증 가능한 관계 명제 하나만 담습니다.

### 타입 지정 리터럴은 컴파운드 텀으로 (재량 아님)

객체가 **날짜·금액·순위·일반 수치**처럼 타입이 있는 리터럴이면, 프로즈 문자열로
두지 말고 **반드시 compact compound term으로** 적습니다. 프로즈로 두면(예:
`"2017.03.08"`, `"126백만원"`, `"3등"`) 엔진이 정렬·임계값·범위 비교에 쓸 수
없고, 단위·구조가 모호해집니다. 컴파운드 텀은 값의 구조를 보존해 두므로, 나중에
`typed-relations.md` 선언만 켜면 **재추출 없이** 비교가 바로 붙습니다.

| 타입 | 형식 | 예시 |
|----|----|----|
| 날짜 | `date(연)` · `date(연,월)` · `date(연,월,일)` | `2017.03.08` → `date(2017,3,8)`, `2020년` → `date(2020)` |
| 순위 | `ordinal(n)` | `3등` → `ordinal(3)` |
| 금액 | `amount(수, "단위")` | `100억` → `amount(100,"억")` |
| 일반 수치 | `number(n)` | `2.5` → `number(2.5)` |

**연도만 아는 경우.** 서지 정보처럼 연도만 있는 값은 `date(2020)` 으로 씁니다
(엔진은 `20200101` 로 정규화해 비교합니다 — 빠진 월·일을 `01` 로 채우는 규칙이
`2030.1` → `20300101` 과 같습니다). 래퍼 없는 맨 `2020` 은 일반 수치와 구별되지
않아 파싱되지 않으니 쓰지 않습니다.

**엔진 지원 현황(과대약속 금지).** `date`·`ordinal`·`number`·`amount` 모두 비교
가능한 스칼라(int64)로 투영됩니다. `number`는 ×1000 고정소수점으로 저장되므로
(소수 3자리), 비교 임계값은 **스케일 정수**로 씁니다 — `V >= 2.0`이 아니라
`V >= 2000` (엔진의 `.dl` 텍스트 파서가 float 임계값을 거부하며, 가드가 명확한
메시지로 잡아줌). `number`·`amount`는 **음수도 받습니다** — 적자 영업이익이나
감소량처럼 음수가 될 수 있는 값은 `number(-672000000)` / `amount(-100,"억")`로 적으면
부호 있는 int64로 비교됩니다. `amount`는 추가로 단위표가 필요합니다.

**참고(직렬화).** 금액 단위는 **항상 따옴표로** 감싸 `amount(100,"억")` 형식으로
씁니다. merge가 단위를 항상 따옴표 형태로 정규화해 저장하고(수의 콤마는 제거), 엔진의
`.dl` 텍스트 파서가 `\"` escape를 지원하므로(wirelog#924) 안전하게 적재됩니다. 단위에
공백·콤마가 있어도 따옴표가 구조를 지켜 모호함이 없습니다 —
`amount(120,"kilometer per hour")`, `amount(2,"달러,센트")`. (`amount(100,억)`처럼
따옴표 없이 써도 merge가 따옴표 형태로 정규화하지만, 따옴표 형식을 쓰는 것이 기본입니다.)

엔티티 객체(기관명·사람·기술명 등)는 compound term으로 감싸지 말고 평범한
이름으로 둡니다. 또한 비교 대상이 될 리터럴 관계는 `policy/attribute-relations.md`
(리터럴 선언)와, 비교가 필요하면 `policy/typed-relations.md`(타입+별칭)에도 함께
등록해야 실제로 투영·비교됩니다.

`confidence`는 source 문서가 해당 subject-relation-object 관계를 얼마나 직접 뒷받침하는지 나타내는 0.00부터 1.00 사이의 점수입니다.
명시적으로 쓰여 있으면 높게, 관계명 선택이나 동일 개념 판단이 필요하면 낮게 둡니다.

## 상태 기준

- `confirmed`: source 문서가 subject-relation-object 관계를 직접 뒷받침합니다.
- `needs_review`: 관계명 선택, 동일 개념 여부, 출처 강도, 충돌 여부를 사람이 봐야 합니다.

LLM 출력은 최종 accepted fact가 아닙니다. 이 파일은 candidate fact 목록입니다.
`needs_review` 행을 만들었다면 같은 판단 이유를 `decisions/open-questions.md`의 알맞은 섹션에 bullet로도 남기십시오.

## 금지 항목

다음 항목은 fact로 저장하지 마십시오.

- API key, private token, password, session secret
- 주민등록번호, 전화번호, 이메일 주소 같은 개인정보 원문
- 고객명, 계정명, 내부 URL처럼 공개하면 안 되는 원문 식별자
- 문서에 없는 배경지식 또는 추론 결과
- "관련 있음"처럼 재사용하기 어려운 넓은 관계명

## 출처 표기

`source`는 항상 `sources/filename.md#section-name` 형식으로 씁니다. 섹션을 특정할 수 없으면 `sources/filename.md`까지는 반드시 남깁니다.

## 정리 원칙

- `sources/`에서 사라진 문서를 근거로 하는 candidate fact는 제거하거나 `decisions/open-questions.md`에 검토 대상으로 남깁니다.
- 같은 의미의 concept가 여러 이름으로 등장하면 자동 병합하지 말고 중복 후보로 기록합니다.
- 기존 페이지의 문장을 바꿀 때는 출처가 여전히 유효한지 먼저 확인합니다.
- `facts/candidates.csv`에 `needs_review`가 하나라도 있으면 `decisions/open-questions.md`에는 최소 한 개 이상의 검토 bullet이 있어야 합니다.
