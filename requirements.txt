# factlog runtime dependency.
# The deterministic logic engine runs on wirelog via pyrewire.
# 1.0.3 is the floor: it bundles/validates wirelog v0.52.0, the first release whose
# .dl parser supports \" escapes (wirelog#924) — needed for always-quoted amount units;
# the upper bound guards against the private-intern API drift documented in the engine module.
pyrewire>=1.0.3,<2.0
